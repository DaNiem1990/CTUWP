<Page
    x:Class="CTUWP.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:CTUWP"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Grid Width="auto" Height="auto">
        <CommandBar HorizontalAlignment="Stretch" Margin="0,0,0,0" VerticalAlignment="Top">
            <CommandBar.Content>
                <Grid/>
            </CommandBar.Content>

            
            <AppBarButton x:Name="Login_Btn"  Icon="Contact2" Click="Login_Btn_Click" Label="Zaloguj się" HorizontalAlignment="Center" Margin="0,0,0,0" VerticalAlignment="Stretch"/>
            <AppBarButton x:Name="Register_Btn"  Icon="Contact2" Click="Register_Btn_Click" Label="Zarejestruj się" HorizontalAlignment="Center" Margin="0,0,0,0" VerticalAlignment="Bottom"/>
            
        </CommandBar>
        <Frame x:Name="frame" Margin="10,40,10,10" HorizontalAlignment="Stretch" VerticalAlignment="Stretch">
            <Frame.Background>
                <ImageBrush ImageSource="/Assets/twicelayout1 (1).png" Stretch="UniformToFill"/>
            </Frame.Background>
        </Frame>
    </Grid>
</Page>
